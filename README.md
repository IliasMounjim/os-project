# A Fairly Simple OS Class Project
### To Build
cmake -B build

cmake --build build

will have built to build/bin

if clangd doesn't work add a symlink to build/compile_commands.json as compile_commands.json at base directory
