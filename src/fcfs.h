#include <string>
#include "strategy.h"

using namespace local;

namespace local {
    namespace strategy {
        namespace FCFS
        {
            extern Strategy evaluate(Schedule s);
        }
    }
}
