#include <string>
#include "policy.h"

using namespace local;

namespace local {
    namespace policy {
        namespace FCFS
        {
            extern Policy evaluate(Schedule s);
        }
    }
}
